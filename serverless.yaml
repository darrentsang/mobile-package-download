service: mobile-package-download-api
 
provider:
  name: aws
  runtime: nodejs18.x
  stage: dev
  region: us-east-1
 
functions:
  app:
    handler: server.handler
    events:
      - http: ANY /
      - http: 'ANY /{proxy+}'
